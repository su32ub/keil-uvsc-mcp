# Keil 调试仿真工具包 — 部署指南

> AI 通过 MCP 协议驱动 Keil µVision(UVSOCK 接口）,对 C51/8051 目标做调试仿真：
> 编译、进/出 Debug、运行控制、断点、读变量/内存/表达式、写内存。共 **22 个工具**。
> 本包开箱即用：`bin\KeilUvscMcp.exe` 为已编译单文件 EXE，零 NuGet 依赖。

---

## 1. 包内容

```
keil-uvsc-mcp\
├── 部署指南.md                 ← 本文
├── bin\
│   └── KeilUvscMcp.exe         ← MCP 服务器主程序(.NET Framework x64 单文件)
├── src\                        ← 源码(可自行重编译)
│   ├── Program.cs              ← MCP 服务器全部实现
│   ├── UVSC_C.h / UVSOCK.h     ← Keil UVSC/UVSOCK 协议头文件(参考)
├── mcp-config\                 ← MCP 注册配置模板(路径已指向本包)
│   ├── .mcp.json               ← Claude Code(项目级)
│   └── claude_desktop_config.json ← Claude Desktop
├── verify\                     ← 10 个冒烟验证脚本(Python 3)
├── docs\                       ← 调研报告 / 方案 / SOP / 验证记录
└── DemoC51\                    ← C51 演示工程(可作为 Simulator 冒烟目标)
```

## 2. 前置条件

| 项 | 要求 | 说明 |
|---|---|---|
| Keil MDK | v5，已安装于本机（如 `C:\Keil_v5`) | `UVSC64.dll` 必须存在于 `C:\Keil_v5\UV4\`；路径由注册表/环境变量自动探测 |
| .NET Framework | 4.x(Windows 10/11 自带） | 仅运行 EXE 需要；重编译用自带 `csc.exe` |
| 调试器 | GLink+ / ULINK 等（或纯 Simulator) | Simulator 无需硬件，适合冒烟验证 |
| Python 3 | 可选 | 仅 `verify\` 脚本需要 |

## 3. 部署步骤

### 3.1 Keil 侧开启 UVSOCK（一次性，必须走 GUI)

1. 打开 µVision(UV4.exe)。
2. **Edit → Configuration → Other** 选项卡。
3. **UVSOCK** 区：勾选 **Enable**,**Port 填 `4823`**，确定。

> ⚠️ 只写注册表 `SocketPort=4823` **不够**——"Enable" 布尔位只能由该对话框写入。
> 设置持久保存在 `HKCU\Software\Keil\µVision5`，以后不用再设。

### 3.2 注册 MCP 服务器

**Claude Code（项目级）**：把 `mcp-config\.mcp.json` 复制到工程根目录（或合并进已有的 `.mcp.json`)，重启会话后工具以 `mcp__keil-uvsc__*` 出现。

**Claude Code（用户级，全局可用）**:
```bash
claude mcp add keil-uvsc "<解压目录>\bin\KeilUvscMcp.exe"
```

**Claude Desktop**：把 `mcp-config\claude_desktop_config.json` 内容合并进
`%APPDATA%\Claude\claude_desktop_config.json`。

> 若把本包移到别的路径，记得同步改配置里的 `command` 路径。

### 3.3 每次调试会话的顺序（不能乱）

1. 确认 UVSOCK 已开（进 Debug 后 `netstat -ano | findstr 4823` 应有 LISTENING)。
2. µVision 打开目标工程。
3. **进 Debug**(Ctrl+F5)。Simulator 无需硬件。
4. AI 侧 `keil_connect`(mode=attach, port=4823)。

**先开 UVSOCK → 再进 Debug → 最后 attach。** UVSOCK 在进 Debug 后才开始监听。

## 4. 重编译（可选）

源码就是 `src\Program.cs` 单文件，用 Windows 自带 csc:

```cmd
C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe /nologo /platform:x64 /out:bin\KeilUvscMcp.exe src\Program.cs
```

> Git Bash 下执行须 `export MSYS2_ARG_CONV_EXCL=*`，否则 `/nologo` 等参数被 MSYS 误转成路径。

## 5. 验证

```bash
cd verify
python verify_read.py     # 读变量   （需 Keil 已在 Debug)
python verify_bp.py       # 断点
python verify_runctl.py   # 运行控制
python verify_build.py    # 编译
# 其余: verify_enter / verify_live / verify_mem / verify_real / verify_space / verify_write
```

每个脚本都是 JSON-RPC over stdio 直连 EXE 的冒烟用例，不依赖 Claude。

## 6. 工具清单（22 个）

| 分组 | 工具 |
|---|---|
| 连接 | `keil_connect` `keil_disconnect` `keil_reconnect` `keil_uvsc_info` |
| 工程 | `keil_load_project` `keil_build`（须非 Debug 模式） `keil_show_window` |
| Debug 会话 | `keil_enter_debug` `keil_exit_debug`（见 7.4) |
| 运行控制 | `keil_run` `keil_stop` `keil_reset` `keil_get_status` `keil_step_instruction` `keil_step_over` |
| 断点 | `keil_set_breakpoint` `keil_clear_breakpoint` |
| 读取 | `keil_read_variable` `keil_read_variables`（批量≤64) `keil_eval_expression` `keil_read_memory` |
| 写入 | `keil_write_memory`（危险，改 RAM/寄存器） |

**无烧录功能**(GLink+/UVSOCK 不提供）。

## 7. 已知约束与坑（实测）

### 7.1 8051 地址编码 `0xSSSS_AAAA`
高 16 位是空间选择：**CODE=`0xFF00`**、**XDATA=`0x0100`**。裸数字地址默认落 CODE 空间（读 0x80 得到的是程序字节而非 SFR)。**最稳妥：先 `keil_eval_expression &变量名` 拿带前缀的地址，再 read/write memory。**

### 7.2 GLink+ 同时只能存在 1 个断点
下新断点前必须 `keil_clear_breakpoint` 清掉旧的。**违反会导致 UVSC 超时并把整个 uVision 卡死**（所有接口返回 Internal Error，只能重启 UV4 恢复）。

### 7.3 GLink+ 运行中不能读变量/设断点
须先 halt(`keil_stop`)。在线充电等场景不要周期 halt 轮询，会打断时序把链路搞掉——用"汇聚点单断点"法（见第 8 节）。

### 7.4 `keil_exit_debug` 会崩掉整个 UV4 进程
要重编译：直接关掉 uVision、以编辑模式重开工程，**不要**调 exit_debug。

## 8. 实战案例：QI 无线充"断开错误码"抓取

以 GPM8F80x6 双线圈工程为例，用单断点抓"充电从哪个 Error 断开":

1. 单断点设在 **`F_QI_POWERTRANSFER_Phase_Error_Handler`**（所有断开路径的唯一汇聚点；空闲 Ping 零命中，不干扰充电）。
2. `keil_reset` → `keil_run`，放上手机，轮询 `keil_get_status` 等 stopped。
3. 命中后读：`bError`（错误类码）+ `bDecode[0]/[1]`(0x02=EPT 包时 [1] 是手机侧断开原因）+ `bTargetStatus`。
4. `bErrorTerminateCounter==1` 说明抓在错误发生第一拍。
5. 错误码表在工程 `User_GPM83_QI_Transmitter.h`:`D_QI_POWERTRANSFER_ERROR_*`(1=A 控制包超时…) 与 `D_QI_EndPowerTransfer_*`(1=充满 3=过温…)。

> 不要设在 `F_ChangSelection_ClosePWM`——启动期 Selection→Ping 会反复误命中。

## 9. 更多文档

- `docs\SOP-Keil-UVSOCK.md` — Keil 侧配置与 JSON-RPC 冒烟命令细节
- `docs\AI接入Keil-C51调试烧录仿真调研报告.md` — 技术选型调研
- `docs\Plan-Keil-C51调试桥.md` — 实现方案
- `docs\验证记录.md` — 全量验证记录（含编译注意事项）
